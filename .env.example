# Webhook Server Configuration
WEBHOOK_HOST=0.0.0.0
WEBHOOK_PORT=8000

# SMTP Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=your-email@gmail.com

# Slack Configuration (use a bot token for webhooks)
SLACK_TOKEN=xoxb-your-slack-bot-token-here

# Mattermost Configuration (optional, if needed for future extensions)
MATTERMOST_URL=https://your-mattermost.com
MATTERMOST_TOKEN=your-mattermost-access-token

# Webhook Configuration (optional, defaults to projects.json)
WEBHOOK_CONFIG=./projects.json